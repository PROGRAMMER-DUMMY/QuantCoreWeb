[browser]
gatherUsageStats = false    # 1. Disables the "Collecting usage statistics" log

[client]
showErrorDetails = false
toolbarMode = "viewer"

#[server]
#headless = true
#enableXsrfProtection = true # 2. Keeps high security against CSRF attacks
## enableCORS = false        # <--- DELETE THIS LINE (It conflicts with XsrfProtection)

[server]
port = None
headless = true

# CRITICAL: These disable the security checks that block your phone
enableCORS = false
enableXsrfProtection = true
